class User {
    constructor(id, name, surname, hourlyRate, userName, password) {
        this.id = id;        
        this.name = name;
        this.surname = surname;    
        this.hourlyRate = hourlyRate;
        this.userName = userName;        
        this.password = password;        
    }
}

export default User;
class Category {
    constructor(id, userId, name, isActive) {
        this.id = id;
        this.userId = userId;
        this.name = name;
        this.isActive = isActive;
    }
}

export default Category;